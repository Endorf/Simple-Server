server {
  listen 80 default_server;

  root {{ document_root }};
  index index.html index.htm;

  server_name {{ server_name }};
  
  location / {
   default_type "text/html";
   try_files $uri $uri/;
  }
}